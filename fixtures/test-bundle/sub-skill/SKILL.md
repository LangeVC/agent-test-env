# Sub-Skill

This is a sub-skill used by the agent-test-env bundle test fixture.
It validates that bundle sub-capabilities are correctly registered and tracked.

## Usage

When installed as part of a bundle, this skill provides no functional instructions.
It exists solely for automated testing.
