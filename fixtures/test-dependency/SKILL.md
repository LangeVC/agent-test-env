# Test Dependency Skill

This skill declares dependencies for testing agent-test-env lock file generation and enforcement.

## Dependencies
- test-skill >=1.0.0
- test-tool >=1.0.0

## Usage

This skill exists solely for automated testing of `agent-test-env lock` and lock enforcement during `agent-test-env install`.
