# Test Runtimes Skill

This skill declares runtime dependencies for testing agent-test-env pre-flight checks.
It requires uv and node to be installed on the host.

## Usage

This skill exists solely for automated testing of `agent-test-env install` runtime validation.
