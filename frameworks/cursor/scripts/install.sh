#!/usr/bin/env bash
source "$(dirname "$0")/_lib.sh"
log "Cursor is a GUI-only application and runs on the host machine."
log "No containerized installation available. See frameworks/cursor/README.md."
exit 0
